export const KM_TO_MILES = 0.621371;

export const RECENT_CATEGORY_ID = "recents";
export const FAVORITES_CATEGORY_ID = "favorites";

export const categoryConstants = {
  RECENT_CATEGORY_ID,
  FAVORITES_CATEGORY_ID,
} as const;
